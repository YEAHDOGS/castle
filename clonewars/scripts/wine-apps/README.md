Drop Windows .exe files here. The folder rides into every VM on the read-only Castle USB share (FAT label CASTLE); the ubuntu-cloud-desktop profile launches each .exe under Wine at login. Binaries are git-ignored.

